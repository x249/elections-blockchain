language: go
dist: xenial
notifications:
  email: false
jobs:
  include:
    - stage: test 
      env:
        - GO111MODULE=on
        - GOFLAGS='-mod vendor'
      go_import_path: github.com/golang/dep
      go: 1.12.x
      script: go test -v -race ./...
