language: go
dist: xenial
notifications:
  email: false
jobs:
  include:
    - stage: test
      go_import_path: github.com/golang/dep 
      env:
        - GO111MODULE=on
        - GOFLAGS='-mod vendor'
      go: 1.12.x
      install: dep ensure
      script: go test -v -race ./...
